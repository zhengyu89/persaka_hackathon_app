import 'package:flutter/material.dart';

class JudgeDashboard extends StatelessWidget {
  const JudgeDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Judge Dashboard'),
      ),
    );
  }
}