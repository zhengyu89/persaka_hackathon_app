# persaka_hackathon_app

Flutter app for the Persaka hackathon platform, with Firebase Authentication and Firestore integration.

## Getting Started

From the project root:

```bash
flutter pub get
flutter run
```

For full teammate onboarding and Firebase setup, see `docs/project-setup.md` and `docs/teammate-setup.md`.
